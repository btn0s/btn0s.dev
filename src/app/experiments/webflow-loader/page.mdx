import WebflowLoader from "../../../components/experiments/WebflowLoader";

export const metadata = {
  title: "Webflow loading animation",
  description: "Recreating the Webflow loading animation.",
  image: "/og-share.png",
  published: false,
};

# Webflow loading animation

This is my attempt to recreate the Webflow loading animation. I used Framer Motion to animate the elements.

<WebflowLoader />
